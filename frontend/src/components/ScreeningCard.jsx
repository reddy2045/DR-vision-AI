import { ArrowUpRight, Eye } from 'lucide-react';
import { Link } from 'react-router-dom';
import Badge, { severityTone } from './Badge';
export default function ScreeningCard({ patient, screening }) { if (!screening) return <div className="card muted-card"><span className="eyebrow">No screening yet</span><h3>{patient.first_name} {patient.last_name}</h3><p>Start a fundus screening to see AI-backed results here.</p><Link className="button secondary" to="/screening/new">Start screening <ArrowUpRight size={16}/></Link></div>; return <div className="card screening-card"><div className="card-top"><div><span className="eyebrow">Latest screening</span><h3>{patient.first_name} {patient.last_name}</h3><span className="muted">{patient.patient_id} · {screening.eye_side}</span></div><Eye size={20} className="teal-icon"/></div><div className="result-line"><Badge tone={severityTone(screening.dr_level)}>{screening.predicted_class || 'Result returned'}</Badge><strong>{Number(screening.confidence || 0).toFixed(2)}%</strong></div><div className="meter"><span style={{ width: `${Math.min(Number(screening.confidence) || 0, 100)}%` }}/></div><div className="card-footer"><span>DR Level {screening.dr_level}</span><Link to={`/patients/${patient.patient_id}`}>View details <ArrowUpRight size={14}/></Link></div></div>; }
